variable "prefix" { type = string }

# Minimal VPC for SageMaker endpoints if needed
# For MVP, SageMaker uses public endpoints

output "placeholder" {
  value = "Networking module placeholder - SageMaker uses public endpoints for MVP"
}
